# PVE Scanner — secrets (chmod 600)
# Copy to /root/pve-scanner/.env on openclaw-vps

# Heimdall service key for /ingest (from hub/.deploy-secrets SERVICE_KEY)
HEIMDALL_SERVICE_KEY=

# Optional: hub URL override
# HEIMDALL_HUB_URL=https://heimdall-hub.eric-c5f.workers.dev